# 3.2.0 修改与部署验证

验证日期：2026-09-22。需求来源：用户提供的《安贞 9.21修改建议(1).docx》。运行代码提交：`bc9ac21`；云端发布目录：`20260922-bc9ac21`。

## 本轮完成

| 文档要求 | 实现与验证结果 |
| --- | --- |
| 病历图片、影像附件 | 上转草稿可上传图片、PDF、DICOM，随申请进入审核和接收详情。每份最多 5 MB，共 10 份；附件服务独立持久化。图片有缩略预览，原文件可获取；不自动解释影像或生成诊断。 |
| 下转接收中心 | 增加独立导航、待接收及已安排列表；成功提交下转后可直接进入接收中心。 |
| 下转信息录入 | 新建患者、编辑交接信息、来源病例导入、选择服务和接收团队，完成接收后沿用同一患者监测时间线。 |
| 东华、嘉和入口 | 完成示例当前患者入口及约定 JSON 文件/粘贴导入，核对后转入上转或下转。切换来源会清空旧患者预览；未接入厂商真实系统。 |
| 工作时间、排序 | 上转详情的工作时间可选择；支持专业匹配、最近接诊时间、平台意向较少排序。含今天，已过时段不可选；保留急症立即对接。 |
| 医务审批、医保事项 | 新申请先待来源医院审核；可退回、补充并保留编号重提，通过后才进入上级接收队列。医保栏目保存人工核对事项。 |
| 手机或微信提醒 | 站内待办、已读状态及直达申请已实现。短信、微信清楚显示未接通、未发送，没有调用外部消息通道。 |

## 自动检查与页面操作

- 本地与 Linux 服务器各 219 项测试通过，包括并发记录冲突、审核状态约束、旧数据迁入、附件大小和类型、文件持久化、患者历史保留，以及延迟保存不能串患者或关闭后来打开的窗口。
- 离线页面构建、内联脚本语法检查、差异格式检查及 ZIP 完整性检查通过。离线文件不提供云端附件上传和真实 AI；联网版本为本轮部署对象。
- 本地使用实际按钮完成：东华病例导入 → PDF 附件 → 真实 AI 匹配 → 最近时间排序 → 点击排班 → 提交审核 → 退回 → 补充重提 → 审核通过 → 上级接收登记 → 消息已读。
- 本地完成：嘉和病例导入 → 新患者信息 → 下转申请 → 成功页进入独立接收中心 → 接收登记 → 首条监测 → 上级视图查看同一日期记录。查看下转医生工作时间正常。
- 本地服务更新重启后，上述申请、审核历史、患者监测与 PDF 附件字节保持一致。
- 云端在 HTTPS 页面实际上传虚拟 PNG、调用真实 AI（返回 8 位候选医生）、选择时段并提交；另一个已打开窗口自动出现新申请，在第二窗口审核后，第一窗口可登记接收并查看审核意见和附件。此过程只使用示例病例和明确标为虚拟的附件。
- 检查窄窗口附件/排班界面及线上桌面详情弹窗；测试页面未捕获 JavaScript 错误。

## 部署与数据保留

- 地址：`https://8.147.71.90/`。已有 HTTP → HTTPS 跳转、证书配置保留；只将实际 Nginx 站点的上传请求上限从 6 MB 调为 8 MB，配置检查通过。
- 私有环境文件内容未改变；没有将密钥、业务文件或部署密码加入 GitHub 或分发包。服务器在发布前备份了环境文件、Nginx 配置、完整持久目录和工作区快照。
- 切换代码前后工作区完全一致：修订号 29、10 条上转、3 位照护患者、3 份下转计划、2 条异常事件。
- 云端页面验收新增 1 条虚拟上转及其附件，最终 11 条上转，修订号 33；没有修改旧记录的业务字段、接收状态和事件，也没有修改原下转数据。执行消息全部已读时，10 条旧记录补充了空的可选 `notifications` 数组。
- 发布后再次重启应用：完整工作区与重启前快照一致，新增申请的通过审核、接收登记及附件仍可读取。原有数据字段逐项保留。服务为 enabled/active；未执行整机重启。
- 公网检查首页、健康接口和新增前端模块返回 200；`.env`、`.env.deploy`、后端源码和存储路径返回 404。上传附件下载字节与本地文件一致。

## 尚需正式对接

真实东华/嘉和 HIS、医保结算、挂号与床位、短信/微信消息及回执均未对接。工作时间和参考余量为预设数据；平台意向不占用真实号源。当前审批为共享工作区的流程角色视图，不具备真实账号、院际权限隔离或人员身份认证；保存协调信息不表示医院已确认接收。
