# 验证记录

## 阿里云实际部署与公网验收（2026-09-20）

访问入口为 **http://8.147.71.90/**。应用运行版本为 Git 提交 `0e93362`，位于 `/opt/anzhen-fde/releases/20260920-0e93362`；`current` 软链接指向该版本。主机为 Alibaba Cloud Linux 4.0.6 x86_64，使用经官方 SHA-256 清单核对的 Node.js v24.21.0。保留已有宝塔 Nginx，仅增加独立站点；应用仅监听 `127.0.0.1:4173`。

- **服务与配置**：systemd 服务 `anzhen-fde` 以非登录 `anzhen` 账户运行，已启用开机启动和异常重启。API Key 单独写入 root 拥有、权限 `0600` 的 `/etc/anzhen-fde/app.env`；仅传输应用配置，未传输 SSH 密码。
- **运行恢复**：向应用主进程发送 SIGTERM 后，进程号更换并在约 **6 秒**恢复健康。验证过进程恢复，未实际重启 ECS，因此不将开机启动配置写成重启实测。
- **首次发布与修复**：实际部署发现软链接启动路径与 Node 模块实际路径不同，入口判断误判导致进程正常退出；现改为真实文件路径比较，新增子进程回归。首次失败回退也修复了不存在的 current 被 `readlink -f` 当成路径、进而恢复成自指向链接的问题；在服务器上用失败版本验证回退后无坏链接和遗留服务文件，再发布正常版本。
- **公网访问**：本机直连、Nginx 代理与外部 HTTP 健康检查均通过；公网首页和 54/54 张医生照片正常。五个配置/后端路径返回 404，异常 Host、Origin 返回 403。只读验收报告位于 `qa/cloud-http-report.json`，不进入 Git。
- **上转实际流程**：公网按钮完成房颤病例匹配 → 黄颖医生详情及七日排班 → 选择 9 月 21 日上午 → 确认转诊 → 接收中心保存协调地点 → 上转记录查询。公网 HTTP 缺少 `crypto.randomUUID` 的兼容问题已增加 `getRandomValues` UUIDv4 后备实现；真实保存产生唯一转诊单，刷新后仍为“已登记待核实”。
- **导诊实际流程**：上传自制虚构病历图片 → 真实文字识别 → 人工核对 → 推荐有图医生 → 郭杰医生详情与七日工时 → 诊室路线 → 基于当前病历的准备资料问答，均成功。
- **下转异常实际流程**：在下级医院视图录入虚构监测异常，真实生成 AI 摘要；上级医院视图显示紧急通知及关键变化、待补充信息与交接重点。原始记录可展开查看。此轮保留一条待复核事件供界面查看，没有实际联系医院。
- **测试与调用**：本地及服务器各 **140 项自动测试通过**；两张验收页面控制台无错误。本轮云端共 **6 次生成请求**：上转匹配 2 次、导诊匹配 1 次、图像识别 1 次、问答 1 次、异常摘要 1 次。其中首轮上转匹配因资料引用校验失败被拦截，页面保留病例，手动重试后成功；其余五次得到可展示结果。模型列表检查另计，不是生成请求。

上述记录验证部署与有限虚构病例流程，不构成临床质量验证。当前使用公网 IP + HTTP；转诊、照护及接收记录依然按浏览器保存在本地，切换网址或设备不会同步原本的记录，尚无跨院数据库、真实账号权限、监测设备或院方业务接口。

## GitHub 与云服务器部署准备（2026-09-20）

增加可选 `PUBLIC_ORIGIN`，以显式来源允许 Nginx 代理访问；本机默认仍只接受 localhost 请求，后端仍只监听回环地址。新增配置格式、公共来源、跨站与伪造转发头回归测试，完整测试 **135 项通过**。

已准备独立服务账户的 systemd 模板、Nginx 站点模板及带健康检查/回退的版本安装器；安装器 `bash -n` 通过。上传清单排除 `.env`、原始 QA 记录、截图、旧压缩包及依赖目录，并核对待提交内容未包含本地 API Key。此记录仅说明代码与部署准备完成；实际云服务器登录、安装及公网验收另行记录，不以模板或本地测试代替。

## 三模块医疗协同服务 v3（2026-09-20）

新增服务首页、上级转下级连续照护与患者导诊；原下级转上级流程保留。使用内置浏览器通过实际按钮完成验证，全部输入为虚构病例或预置场景。

- **首页与数据保留**：三功能入口可切换；服务更新和刷新后，原有 4 条上转记录保留。
- **下转全流程**：王先生下转申请 → 下级登记康复接收地点与服务计划 → 医生详情七日工时 → 录入紧急监测 → 上级 AI 通知 → 人工复核登记 → 下级查看处理回传。按日期筛选能查看同一天上午与下午的两条记录。
- **异常摘要**：两次真实生成，第二次在同一事件上重新整理，未创建重复事件。首次输出偏长且混淆 UTC 与北京时间；修复为入模前统一北京时间、强调最新记录相对基线的关键变化。复测显示简洁摘要、两条关键变化、两项待补充及交接重点，无原时区误报。原始事件保留可展开查看。
- **导诊全流程**：上传自制虚构病历图片 → 真实图像识别 → 手工核对 → 真实医生推荐 → 王昌会医生详情和七日工时 → 科室路线 → 围绕该医生的真实问答，全部成功。未经核对不能推荐；新选图片会禁用推荐和问答、清除旧推荐，移除图片后恢复文字操作。
- **原上转兼容**：在独立离线页面以规则匹配检查医生详情，新增七日工时正常，原紧急转诊下一步按钮可见。此次没有为原上转另行发起付费匹配或新增上转单。
- **布局与错误**：1440px 桌面首页、640px 窄屏首页/下转/导诊检查通过，窄屏无页面水平溢出；未将窄屏结果表述为 390px 手机验收。导诊浏览器控制台无错误。
- **自动检查**：127 项测试全部通过，覆盖原匹配/存储、图像校验和大小限制、两个新 API 的并发与限速、时间归一、下转输入、导诊请求取消与图片核对状态。离线文件已重新构建，嵌入脚本语法通过。

本轮共 **5 次真实生成调用**：病历识别 1 次、导诊匹配 1 次、导诊问答 1 次、异常摘要 2 次，均成功返回。自动测试使用本地替身，不额外消耗生成额度。后台服务已同步新版，沿用原 `127.0.0.1:4173` 网址。

上下级视图共用当前浏览器的本地数据；此版未接入真实跨院账号权限、HIS、监测设备、排班、床位或院内导航。排班与路线均为预置安排；处理登记不代表真实联系或临床处置。操作与数据边界见 [三模块使用说明](care-platform.md)。

## 本地服务常驻与自动恢复（2026-09-20）

处理前 `127.0.0.1:4173` 无监听、HTTP 无法连接；原启动方式为临时前台进程。现安装当前用户的 macOS LaunchAgent，保持原网址与仅本机访问；运行文件位于用户 Application Support 的独立版本目录，密钥仅复制到权限为 0600 的本地配置文件。启用登录自动加载、退出自动重启与 5 秒重启节流。更新失败保留回滚路径，双击启动脚本会同步原项目的代码与配置。

实际验收：

- 后台进程由 launchd 管理（父进程为 1），启动命令完成退出后服务仍可访问。
- 发送 SIGTERM 后更换 PID 并恢复健康，实测约 **0.4 秒**；发送 SIGKILL 后再次更换 PID 并恢复健康，实测约 **5.3 秒**。
- 两个重启命令交叠时，第二个被管理锁拦截；第一个正常更新，运行版本目录与状态元数据一致。
- 首页、54 张医生照片、健康端点和配置加载正常；`.env` 与后端源码仍返回 404，端口仍仅监听 `127.0.0.1`。
- 用户原浏览器刷新后，4 条转诊记录及状态均保留。
- 自动测试 **64 项通过**，新增健康检查不调用上游、不泄露配置、沿用 Host/Origin 限制；启动脚本和系统服务配置语法通过。

本轮没有付费模型调用。登录自启已配置；未实际执行注销、重启电脑或睡眠/唤醒测试，不将其描述为实测通过。睡眠、关机期间本机网址不可用，外部模型服务可用性也不由本地进程重启保证。操作说明见 [后台服务说明](local-service.md)。原始恢复计时记录保存在 `qa/local-service-report.json`，不进入交付包。

## 医生详情下一步按钮可见性修复（2026-09-19）

用户截图暴露电脑端长内容弹窗的操作栏处于可视区域下方。将底部操作栏吸底样式从手机断点提升为通用规则，保留弹窗内部滚动。使用独立离线页面从实际按钮验证：1280×720 下弹窗内容高度 825px、可视高度 655px，打开医生详情即能看到“复核并立即对接”（按钮底边 670px），点击后正常进入确认转诊信息。此次仅验证布局与页面跳转，无模型调用、未创建转诊记录。离线文件与交付包同步更新。

## 实际按钮完整流程验收（2026-09-19，本轮最新）

使用用户当前的内置浏览器，通过页面实际按钮执行病例输入、真实模型匹配、医生详情、复核提交、接收协调和记录查询。两标签页交叉操作，共 7 次真实生成请求：5 次匹配（其中修复前 2 次引用校验失败）、2 次问答。失败时病例保留，点击重试可继续；未使用接口替身代替本轮流程。

房颤、急性心梗、术后康复三类病例完整走通；另验证不同来源医院创建独立申请，最终 4 条记录均为“已登记待核实”，刷新后保留。检查了返回修改、日期变化后时段重选、必填项、复核勾选、同源去重、跨页同步及连续问答。有效窄屏宽度为 640px，记录与详情无水平溢出；本轮未将请求的 390px 尺寸当作已生效。

本轮修复：模型改选医生原文条目，由服务端解析引用；申请去重纳入来源医院和发起医生；跨标签页保存合并记录并同步状态，防止覆盖。新增 UUID 单号及长单号换行。最新自动测试 **63 项通过**。细节与逐项结果见 [本轮验收报告](ui-acceptance-2026-09-19.md)。以下保留此前不同验证阶段的记录，其调用数与测试数不代表本轮新增。

## 工作台界面整理（2026-09-19）

移除前台模型品牌、模式切换、配置状态及演示标记，统一病例、匹配、问答和协调登记文案。日期时段表达为接诊意向，协调登记后为“已登记待核实”；未同步院方的数据仍在相关操作处简洁说明。旧记录只转换显示标签，不改写已保存内容。

本次验证：39 项自动测试通过；10 组浏览器检查通过，覆盖桌面/手机、医生详情、普通/急症转诊、协调登记、问答、失败/未配置及旧记录。离线工作台另行检查通过，零 API 请求。当前用户浏览器已刷新至新版。此次界面复测未调用付费模型；以下真实调用记录来自上一个接入验证阶段。

2026-09-19，Node.js 24、本地 Chrome、`kimi-k3`，`reasoning_effort: low`。仅使用虚拟病例。

## 真实 API 与页面测试

密钥鉴权通过，账户模型列表包含 `kimi-k3`。共发起 **9 次真实生成请求，均 HTTP 200**：6 次匹配、3 次问答。接口成功不等于内容合格；发现的问题与修正另列如下。

| 场景 | 观察结果 | 耗时 |
| --- | --- | --- |
| 房颤择期转诊 | 返回 8 位有图医生；完成选日期/时段、确认、模拟接收、记录查看 | 首轮 29.1 秒，提示修正后 26.3 秒，最终 27.7 秒 |
| 急性心梗 | 识别急症，走立即对接并完成模拟接收，普通日期不可选 | 32.6 秒 |
| 术后康复 | 返回康复方向候选，保持择期流程 | 10.2 秒 |
| 骨科病例夹带强制高分指令 | 未推荐心血管医生 | 6.6 秒 |
| 所选医生问答及追问 | 带入病例、医生与可见历史，解释虚拟排班和信息补充 | 13.7 / 8.9 秒 |
| 评分解释复测 | 准确说明 45 / 30 / 15 / 10 四项权重及排班、学历不参与评分 | 8.9 秒 |

详情与转诊流转没有额外调用模型。桌面照片加载、390px 手机布局通过；页面异常为 0。截图使用真实返回内容复现，不增加生成调用。实际耗时受账户、网络和模型服务影响。

## 发现并修正

1. 首轮问答曾把虚拟余号误列为评分因素：匹配与问答现在共用明确评分规则，并禁止学历、职称、虚拟排班参与评分。
2. 曾把一般心律失常评估过度解释为消融能力，且提示词单独约束仍未守住技术分上限：增加服务端证据限制，缺明确技术证据者技术项最多 10 分，只有门诊归属且无详细专长者为 0 分；替换越界理由，重算总分并排序。
3. 删除从“特需门诊”推断安排便利性的匹配理由。补充否定和历史病例回归，避免把“术后随访，不评估再次消融”当作当前消融需求。

保存响应的独立离线回放 **5 组通过**，逐位检查全部 8 位候选；无消融证据者技术 24→10、总分 82→68，有明确证据的其余 7 位分值保持不变。最终真实页面调用再次验证技术上限、分项加总、照片和详情显示。原中间测试的人工复核失败已记录，未将其误列为质量通过。

## 代码与交付包

- 接入阶段 Node 自动测试：**39 项通过**，覆盖规则与排班、模型参数、ID / 引用 / 分项校验、技术证据限制、异常响应、取消与超时、密钥文件及跨站请求拦截；本轮扩展至上文的 63 项。
- 接入阶段另有模拟 API 的浏览器 11 组与离线标志 2 组通过，覆盖失败重试、HTML 转义、取消旧请求、无 Key 页面、显式离线零请求等。它们用于流程验证，与上述真实调用区分。
- 发布 ZIP 检查 CRC、文件哈希、54 张照片齐全；随包 `.env` 密钥为空，本地已填 Key 不进入压缩包。
- 本地原始记录位于 `qa/live-kimi-results.json`、`qa/live-kimi-regression.json`、`qa/live-kimi-final.json`、`qa/evidence-regression-results.json`。包含虚拟病例与模型可见输出，不包含密钥或私有思考；原始 QA 文件不进入交付包。

当前结论是 Demo 的连接与演示流程可用。有限样例和词汇证据规则不构成临床质量验证，未对接真实医院排班或转诊系统。基础问答尚不读取页面上一轮具体评分明细，因此能解释规则和公开专长，不应编造某次具体分值。

`npm run check:kimi` 只检查连接与模型；`npm run check:kimi -- --live` 会再产生两次生成请求，无需在已完成测试后重复运行。
## 2026-09-20 云端共享记录验收

部署地址为 `http://8.147.71.90/`，运行版本 `bd87910`。本轮把上转申请及接收、下转计划及安排、连续监测和紧急通知从浏览器保存迁到服务端。原有数据合并，不清空浏览器备份；同网址访问者共享记录，约每 5 秒同步，尚无账号或机构权限隔离。

- 本地和云端 Linux 各执行 **171 项自动测试，全部通过**。覆盖持久化、版本冲突重试、独立客户端并发不覆盖、失败保留输入、非法旧记录不漏导、修改下转计划重新协调、备份损坏恢复与接口访问限制。
- 合并两个原网址已有数据：5 条上转、3 位照护患者、23 条监测、1 份下转计划、2 条紧急事件（1 条已处理、1 条待处理）。逐项核对原记录 ID 和监测 ID 保留。
- 云端真实按钮操作：录入李先生常规监测，另一窗口自动出现；上级提交下转后，由另一窗口下级视图登记接收，成功保存。
- 云端真实 AI 匹配 1 次：术后康复病例返回唐海沁，70 分；选 9 月 22 日下午，复核提交后，另一窗口接收队列自动出现，完成协调登记。
- 验收后服务器共 6 条上转、3 位照护患者、24 条监测、2 份已登记安排的下转计划、2 条紧急事件。独立 HTTP 访问无需浏览器存储也读取同一份数据。
- 重启 `anzhen-fde` 服务前后完整快照相等，记录版本保持 7；服务器配置文件字节内容未改变。此次验证为应用服务重启，未重启整台云主机。
- 应用配置为私有 `/etc/anzhen-fde/app.env`，共享数据及自动前版本备份为 `/var/lib/anzhen-fde/workspace*.json`，文件权限均为 0600。部署更新不替换数据目录；额外保留迁移前、迁移后与验收后的私有快照。
- `.env`、后端源码、数据文件直链均返回 404；运行所需 API Key 已在服务器私有配置内，SSH 密码未上传。源码与 ZIP 扫描未发现本地实际密钥，交付包内 `.env` 仍为空模板。
- 离线 HTML 构建及内嵌模块语法检查通过，ZIP 重建成功。导诊的图像识别与问答沿用此前部署功能，本轮未再次调用；导诊当页原始上传及对话不作为共享业务档案保存。

以上是虚构业务数据的协同流程验收，未对接院方系统，不代表真实医院接收、排班或临床质量验证。从旧备份恢复后，已打开的浏览器应重新加载再继续操作。
